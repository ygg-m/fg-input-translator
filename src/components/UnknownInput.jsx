import { ReactComponent as SVG } from "../images/inputs/UnknownInput.svg";

export const UnknownInput = () => {
  return (
    <div className="motion-input-container">
      <SVG />
    </div>
  );
};
